# Specify the source for gems
source "https://rubygems.org"

# Gem for creating fast and lightweight APIs
gem 'sinatra'

# The web server that will run our Sinatra app
gem 'puma'

# For database interaction
gem 'sequel'
gem 'sqlite3'